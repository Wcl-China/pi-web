# Jiyun Web 从源码安装到全局指南

> 适用环境：Windows 11、PowerShell、NVM for Windows  
> Jiyun Web：`0.9.0`  
> 配套 Jiyun coding agent：`0.85.1`  
> 默认地址：<http://127.0.0.1:30142>

## 1. 项目关系

推荐保持下面的同级目录结构：

```text
jiyun-agent/
├── jiyun/
│   └── packages/coding-agent/
└── jiyun-web/
```

Jiyun Web 在自己的 Next.js 进程内使用 Jiyun coding-agent SDK，不需要先启动 `jiyun` 命令。两者默认共用 `~/.jiyun/agent` 下的模型、凭据、插件、技能和会话。

## 2. 前置检查

项目要求 Node.js `>= 22.19.0`：

```powershell
node --version
npm --version
```

本项目验证使用 Node.js `v24.11.1`。

## 3. 首次构建配套 Jiyun

进入 Jiyun monorepo：

```powershell
cd C:\Users\37886\Project\21_软件智能体\jiyun-agent\jiyun
npm install
```

如果各 workspace 尚未构建，执行一次完整构建：

```powershell
npm run build
```

如果底层包已经构建，只需重新构建 coding-agent：

```powershell
npm run build --workspace=@jiyun-ai/jiyun-coding-agent
```

## 4. 安装 Jiyun Web 依赖

```powershell
cd C:\Users\37886\Project\21_软件智能体\jiyun-agent\jiyun-web
npm install
```

Jiyun Web 的 `package.json` 使用本地依赖：

```json
"@jiyun-ai/jiyun-coding-agent": "file:../jiyun/packages/coding-agent"
```

项目 `.npmrc` 启用了 `install-links=true`。npm 会把本地 Jiyun coding-agent 按真实 npm 包复制到 `jiyun-web/node_modules`，避免 Next.js 构建穿透 Junction 进入 Jiyun monorepo。

如果之后修改并构建了 Jiyun coding-agent，刷新 Web 项目中的复制包：

```powershell
npm install --force
```

## 5. 构建 Jiyun Web

```powershell
npm run typecheck
npm test
npm run lint
npm run build
```

生产构建会生成 `.next/`。该目录是 Next.js 的运行产物，不提交 Git，但会被 `npm pack` 收入安装包。

## 6. 停止旧实例

如果已有 `jiyun-web` 正在运行，先在对应终端按 `Ctrl+C`。否则 Windows 可能因为文件占用导致卸载或覆盖失败。

检查默认端口：

```powershell
Get-NetTCPConnection -LocalPort 30142 -State Listen -ErrorAction SilentlyContinue
```

## 7. 检查并卸载旧的全局包

```powershell
npm list -g --depth=0 | Select-String "jiyun-web"
```

如果存在旧版本，先卸载：

```powershell
npm uninstall -g @jiyun-ai/jiyun-web
```

卸载程序不会删除 `~/.jiyun/agent`，模型、凭据和会话仍会保留。

## 8. 方式一：复制安装（推荐日常使用）

先生成 tarball：

```powershell
cd C:\Users\37886\Project\21_软件智能体\jiyun-agent\jiyun-web
npm pack
```

包名由 `package.json` 的 `name` 和 `version` 生成：

```text
@jiyun-ai/jiyun-web + 0.9.0
→ jiyun-ai-jiyun-web-0.9.0.tgz
```

安装并清理 tarball：

```powershell
npm install -g .\jiyun-ai-jiyun-web-0.9.0.tgz
Remove-Item .\jiyun-ai-jiyun-web-0.9.0.tgz
```

复制安装会把程序、`.next` 和配套 Jiyun SDK 放入 npm 全局目录。之后移动源码目录不会影响已经安装的命令。

## 9. 方式二：链接安装（推荐频繁开发）

如果需要频繁修改 Jiyun Web，可直接链接源码目录：

```powershell
cd C:\Users\37886\Project\21_软件智能体\jiyun-agent\jiyun-web
npm uninstall -g @jiyun-ai/jiyun-web
npm install -g .
```

链接安装后，全局包会以 Windows Junction 指向当前源码目录。修改源码后只需重新构建：

```powershell
npm run build
```

链接安装的源码目录不能随意改名或移动，否则全局命令会断链。

## 10. 验证和启动

```powershell
npm list -g --depth=0 | Select-String "jiyun-web"
jiyun-web --help
jiyun-web
```

正常情况下会自动打开：

```text
http://127.0.0.1:30142
```

Jiyun Web 默认使用 `30142`，Pi Web 默认使用 `30141`，因此可以在两个终端中同时运行：

```powershell
# 终端 1
pi-web

# 终端 2
jiyun-web
```

不自动打开浏览器：

```powershell
jiyun-web --no-open
```

临时指定其他端口：

```powershell
jiyun-web --port 8080
```

## 11. 修改源码后的重新安装

复制安装模式：

```powershell
cd C:\Users\37886\Project\21_软件智能体\jiyun-agent\jiyun-web
npm run build
npm uninstall -g @jiyun-ai/jiyun-web
npm pack
npm install -g .\jiyun-ai-jiyun-web-0.9.0.tgz
Remove-Item .\jiyun-ai-jiyun-web-0.9.0.tgz
```

链接安装模式只需：

```powershell
npm run build
```

## 12. 常见问题

### 仍然看到旧的 π Pi Web 标题

1. 确认旧 `jiyun-web` 进程已经停止。
2. 确认 `npm list -g` 显示 `@jiyun-ai/jiyun-web@0.9.0`。
3. 重新启动 `jiyun-web`。
4. 浏览器使用 `Ctrl+F5` 强制刷新。
5. 如果安装过 PWA，可在浏览器开发者工具中清理旧 Service Worker 后重新打开。

### `jiyun-web` 命令找不到

重新打开 PowerShell，再检查：

```powershell
Get-Command jiyun-web
npm root -g
```

### 提示端口被占用

```powershell
Get-NetTCPConnection -LocalPort 30142 -State Listen
jiyun-web --port 30143
```

### 修改 Jiyun 后 Web 没有变化

```powershell
cd C:\Users\37886\Project\21_软件智能体\jiyun-agent\jiyun
npm run build --workspace=@jiyun-ai/jiyun-coding-agent

cd ..\jiyun-web
npm install --force
npm run build
```

## 13. 卸载

```powershell
npm uninstall -g @jiyun-ai/jiyun-web
```

除非明确要删除凭据和会话，否则不要删除 `~/.jiyun/agent`。
